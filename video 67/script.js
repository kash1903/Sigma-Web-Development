console.log("Hellow World!");

// document.body.firstChild  => gives you the first child in the body (usally a text so returns #text)

// document.body.firstElementChild => gives you the first child which is a Element (In this case return div.container)

// document.body.firstElementChild.Children[1] => (Returns box2 as as index based counting )
